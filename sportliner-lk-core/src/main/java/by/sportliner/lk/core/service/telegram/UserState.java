package by.sportliner.lk.core.service.telegram;

public enum UserState {
    AWAITING_OPERATION_SELECTION,
    AWAITING_BRANCH_OFFICE,
    AWAITING_CHILD_NAME,
    AWAITING_PHONE,
    AWAITING_DIAGNOSIS,
    AWAITING_DATE
}
