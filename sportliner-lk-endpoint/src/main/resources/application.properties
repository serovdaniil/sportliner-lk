spring.config.import=classpath:/application-build.properties

server.port=8084

spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Serov231969

# TODO review circular dependencies
spring.main.allow-circular-references=true

jwt.secret.password=password
jwt.secret.salt=salt

logging.level.by.sportliner.lk=DEBUG

# bean-validation temporary disabled (also was not used before), as causing null-violations in endpoints for some CRUDs
spring.jpa.properties.javax.persistence.validation.mode=none
